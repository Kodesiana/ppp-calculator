{
    "name": "ppp-calculator",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "format": "prettier . --write",
        "start": "wrangler pages dev",
        "deploy": "wrangler pages deploy"
    },
    "dependencies": {
        "jszip": "^3.10.1",
        "papaparse": "^5.5.0",
        "prettier": "^3.4.2"
    },
    "devDependencies": {
        "wrangler": "^3.60.3"
    }
}
